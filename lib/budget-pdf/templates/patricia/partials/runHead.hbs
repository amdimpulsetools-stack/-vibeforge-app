{{!-- Partial · cabecera de continuación (páginas 2+). --}}
      <header class="run-head">
        <div class="rh-logo"><img src="{{org.logo_url}}" alt=""></div>
        <div class="rh-meta">Presupuesto · {{doc.title}} · <b>{{budget.code}}</b></div>
      </header>
